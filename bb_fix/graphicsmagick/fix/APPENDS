inherit autotools
EXTRA_OECONF += "--enable-static=no --enable-shared=yes --with-png=yes"

FILES:${PN}-dev:remove += " \
    ${datadir}/GraphicsMagick-${PV}/config \
    ${libdir}/GraphicsMagick-${PV}/config \
"

FILES:${PN} += " \
    ${datadir}/GraphicsMagick-${PV}/config \
    ${libdir}/GraphicsMagick-${PV}/config \
"

